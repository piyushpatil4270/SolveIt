import {ChartData,ChartOptions} from "chart.js"

export const SubmissonChart=()=>{

  /*import { Chart } from 'chart.js';
    const myOptions: Chart.ChartOptions = {
    title: {
    display: true,
    text: 'My title',
    fontSize: 16,
    },
    };
   */
    return(
        <div>
            SubmissionChart
        </div>
    )
}